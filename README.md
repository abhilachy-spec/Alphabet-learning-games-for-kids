# Alphabet Adventure App

Android Studio project containing the Alphabet Adventure HTML game.

Open this folder in Android Studio, sync Gradle, then use Build > Build APK(s).
The game is bundled as app/src/main/assets/index.html and can run offline.
